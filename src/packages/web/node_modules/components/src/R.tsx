import { colors } from '../../blocks/studio-store-ecommerce-theme/src/colors'
import { fonts } from '../../blocks/studio-store-ecommerce-theme/src/fonts'
import {
    images, signUpImages, loginImages, authImages, catalogueImages, connectedAccountsImage, contactUsImage, categoriesSubcategoriesImage,
    forgotPasswordImage, helpCenterImage, interactiveFaqsImage, locationImage, liveChatImage,
    notificationImage, orderDetailViewImage, orderManagementImage, orderSummaryImage,
    otpInputAuthImage, paymentsImage, profileBioImage, profileFaqImage,
    pushNotificationImage, searchImage, shoppingCartImage,
    whishlistImage
} from '../../blocks/studio-store-ecommerce-theme/src/images'
import { dimensions } from '../../blocks/studio-store-ecommerce-theme/src/dimensions';

const R = {
    colors, fonts, images, signUpImages, dimensions, loginImages, authImages, catalogueImages, connectedAccountsImage, contactUsImage, categoriesSubcategoriesImage,
    forgotPasswordImage, helpCenterImage, interactiveFaqsImage, locationImage, liveChatImage,
    notificationImage, orderDetailViewImage, orderManagementImage, orderSummaryImage,
    otpInputAuthImage, paymentsImage, profileBioImage, profileFaqImage,
    pushNotificationImage, searchImage, shoppingCartImage,
    whishlistImage
};

export default R;